# Environment files — contain real secrets (DB credentials, API keys, JWT secret).
# Matches .env in any directory (backend/.env, frontend/.env, etc.)
.env
.env.*
!.env.example

# Dependencies — installed via package.json, never committed.
node_modules/

# User-uploaded content — real user files, not source code.
backend/uploads/*
!backend/uploads/.gitkeep
